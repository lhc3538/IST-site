$(document).ready(function(){
	/* ---------- Datable ---------- */
	$('#datatable').dataTable();
	
	/*if ( $.fn.dataTable.isDataTable( '.datatable' ) ) {
		table = $('.datatable').DataTable();
	}
	else {
		table = $('.datatable').DataTable( {
			paging: false
		} );
	}*/
});